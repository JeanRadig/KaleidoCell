name: kaleidocell_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch --index-url https://download.pytorch.org/whl/cu124  # CUDA 12.4 wheel
      - ipykernel
      - -e .
